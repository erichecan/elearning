# Phase 6 - Day 6 执行记录（2026-03-01）

## 完成项
- 儿童端奖励兑换闭环补齐：
  - 新增儿童端页面：`RewardsScreen`（可兑换项 + 实时余额 + 兑换历史）。
  - 首页“奖励系统”卡片可直接进入兑换页。
- 后端奖励链路补齐：
  - `redeem` 改为事务化扣币：
    - 校验奖励存在/激活/归属。
    - 校验余额充足。
    - 写入 `redemptions`（`granted`）。
    - 写入 `token_ledgers` 负数流水（`reward_redeem`）。
  - 新增历史接口：`GET /api/rewards/history?childId=...&limit=...`。

## 验证
- backend `npm run build` 通过。
- `npm run smoke:corewords` 回归通过（AAC 主链路保持通过）。

## 备注
- 当前前台兑换页使用 `activeChildId`；未设置时会尝试自动选择第一个 child。
- 全量前端 `npm run build` 仍受历史遗留 TS 报错影响（非本次新增）。

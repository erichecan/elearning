# 产品需求文档（PRD）· 循证修订版

> 基准：`PRD_补充版.md`（原始输入，保持不动）
> 修订依据：`docs/AAC_RESEARCH_EVIDENCE_2026-06.md`（循证研究报告）、`docs/FLOW_DIAGRAMS.html`（运行逻辑 & 用户行为流程图）
> 修订日期：2026-06-06
> 目标用户：自闭症儿童（主）、照护者/治疗师（次）

## 证据档位标记说明

每条循证标注用以下记号，未标记的内容沿用原 PRD：

| 记号 | 含义 |
|------|------|
| 🟢 | 强证据（多个同行评议系统综述/meta 分析） |
| 🟡 | 有限证据（小样本/单受试/需外推） |
| 🔵 | 专家/机构共识（权威指南，非 RCT） |
| ⚪ | 商业实践（产品官网/弱设计研究） |
| ⚠️ | 工程默认值（无循证出处，合理但需后续验证） |

---

## 0. 本次修订摘要（相对 `PRD_补充版.md` 的关键变更）

| # | 变更点 | 原版写法 | 循证修订 | 档位 / 出处 |
|---|--------|----------|----------|------------|
| 1 | **核心词位置** | "支持拖动排序与锁定" | 运行时**位置永久固定**；拖动排序**仅限家长配置态**，保存后全局锁定，**不在儿童使用中改变位置** | 🟢 Thistle 2018；LAMP |
| 2 | **AI 推荐** | "Context-Aware 预测：按时间/地点/历史推荐词汇" | AI **只做高亮 + 推荐句 + 自动朗读，绝不移动词的位置或重排网格** | 🟢 运动规划 |
| 3 | **场景切换** | "词库自动切换" | 切换只换 **fringe（情境词）与 VSD 背景**，**核心词网格保持不变** | 🟢 运动规划 + VSD EBP |
| 4 | **网格密度** | "2×2 至 10×10 按能力分级" | 网格大小按"**能看见 + 能点到**"定，**不按认知能力降级**；默认给健壮（较大）词汇，运动/视觉障碍优先用适配手段而非缩小网格 | 🔵 ASHA/AssistiveWare |
| 5 | **句子构建** | 句子条与预存未区分主次 | **逐词造句为主路径**；预存"快捷短语"仅作高频社交/紧急**补充，绝不替代** | ⚠️/共识 |
| 6 | **flashcard** | 独立学习模块 | flashcard 词条 = AAC 词条**同一套**；学完能**立即在句子条用出来**；教学**嵌入自然情境的辅助语言示范**，非孤立刷卡 | 🟢 Pope 2024；O'Neill 2021 |
| 7 | **词汇起点** | 未定起始词表 | 起步 **Universal Core 36 词**，但**全量始终可见**，不"逐步解锁"；同时支持**个体化** fringe 配置 | 🔵 Project Core；🟡 Carnett 2023 |
| 8 | **能力门槛** | 鉴权/能力分级 | **不设"先决技能/能力门槛"**才给词汇，尽早提供健壮词汇 | 🔵 ASHA |
| 9 | **80% 归因** | （`CORE_WORDS_BRIEF.md` 归 ASHA） | 更正为 **Vanderheiden & Kelso 1987** | ⚠️ 溯源 |
| 10 | **Fitzgerald 配色** | 作为设计要求 | 保留（行业惯例、利于一致性），但**不作为循证卖点**；一致性 > 配色方案本身 | ⚪ 无疗效证据 |

---

## 1. 首页信息架构与核心交互（儿童端）

> 整体布局沿用原 PRD（顶部奖励/情绪控制室/at school/at home/任务提醒；中部句子条 + Core Words Grid；底部 math/flashcards/绘本/settings）。以下为循证修订点。

### 1.1 顶部栏
- `奖励系统`、`情绪控制室`、`任务提醒`：沿用原 PRD。
- `at school` / `at home`：家长上传场景照片，绑定 VSD 热区。
  - 🟢 **修订**：场景切换时**仅切换情境词（fringe）与场景背景图**，**核心词网格的词与位置必须保持不变**。理由：固定位置支持运动规划（Thistle 2018），VSD 对 3-8 岁自闭症幼儿是循证实践（Patenaude 2025）。

### 1.2 中部核心区域
- `句子条（Sentence Strip）`：显示已选 core words 拼成的一句话，支持 TTS 朗读与一键清空、删除最后一词。
  - ⚠️ **修订**：句子条**逐词造句是默认主路径**；预存快捷短语是补充入口（见 §2.1）。
- `Core Words Grid`：核心词网格，Fitzgerald 配色。
  - 🟢 **修订**：**位置永久固定**，固定运动记忆锚点。运行态**禁止任何自动重排/AI 重排/拖动改位**。
  - 🔵 **修订**：网格密度（如 5×9、7×11）按"**用户能看见 + 能点到**"选择，**不因"认为孩子能力不够"而降级为小网格**；默认提供健壮词汇并**始终可见**。
  - ⚪ Fitzgerald 配色：选定一套色表后**全局保持一致**（动词/代词/名词/形容词/介词/疑问词分色），但不宣称其有疗效证据。

### 1.3 底部功能入口
- `math` / `flashcards` / `绘本` / `settings`：沿用原 PRD。
  - 🟢 **修订**：`flashcards` 与 AAC 词库**共用同一套词条/符号**（见 §2.5）。

---

## 2. 功能需求（循证细化）

### 2.1 智能辅助沟通（Smart AAC）

1. **Core Words 网格系统**
   - 🟢 固定核心词位置，支持多密度网格；**运行态位置不可变**。
   - 🔵 起始词表：**Universal Core 36 词**（动词/形容词/代词/介词方位/疑问词全词类覆盖），全量始终可见。
   - 🟡 同时支持**个体化**：在不破坏核心布局前提下，家长可加个人偏好的 fringe 词（理由：核心词专门干预证据弱，应个体化选词 + 教多词类，Carnett 2023）。
2. **Sentence Strip 句子拼接**
   - 点词自动拼句、TTS 朗读、中英文混排。
   - ⚠️ **逐词造句为主路径**。
3. **预存快捷短语（新增，明确定位）**
   - ⚠️/共识 提供**少量**高频社交/紧急整句（如"我要…""不要""帮我""停""你好""谢谢""我要上厕所"）。
   - **约束**：预存短语**只作加速补充，绝不取代逐词造句**（理由：预存句提升速度但词预测/整句会增认知负荷，且不利语言发展自主性）。
4. **AI 辅助（重定义边界）**
   - 🟢 **允许**：高亮推荐词（**不挪位**）、生成推荐句供选、点击自动朗读、按场景**显示/隐藏整组 fringe**。
   - 🟢 **禁止**：运行时自动**重排核心词位置**、核心网格"动态变化"、基于历史"智能换位"。
5. **VSD 场景交互**
   - 🟢 家长拍场景照片、设热区与语句触发；VSD 作为**幼儿/低能力儿童的切入显示**，与固定核心词网格并存（hybrid）。

### 2.2 生活技能与执行功能（LifeOS）
> 沿用原 PRD：交互式视觉日程、任务拆解（子步骤图/GIF）、Time Timer 风格视觉计时、首页任务提醒、Focus 模式。
- 🟢 **补充**：进入 Focus 模式后**底部仍保留 AAC 核心词/紧急按钮**（沟通权利不应被任务流程剥夺，呼应 ASHA"无门槛"立场）。

### 2.3 情绪调节与行为干预（EmoGuard）
> 沿用原 PRD：Zones of Regulation 蓝/绿/黄/红自检、情绪控制室入口、生物反馈（P1-P2 可选）。无新增循证约束（本轮未专项研究情绪模块，列为开放项）。

### 2.4 奖励系统（Token Economy）
> 沿用原 PRD：完成任务/正确使用 AAC 自动奖励、阈值兑换、家长配置规则。
- ⚠️ **补充**：奖励应强化"**功能性沟通发生**"本身，而非仅任务完成（与辅助语言示范/强化沟通动机一致，见流程图核心回路）。

### 2.5 学习模块（重点修订）

1. **数学模块（math）**：沿用原 PRD（低/中高阶分级、视觉辅助、完成触发奖励）。
2. **Flashcards 模块（循证重构）**
   - 🟢 **词条同源**：闪卡的词 = AAC 词库的词条/符号**同一套**，学完的词**立即可在句子条用出来**（学用一体）。
   - 🟢 **嵌入辅助语言示范**：闪卡学习设计为"沟通伙伴边说边点符号"的 aided language modeling，而非孤立看图刷卡（理由：打包式多组分干预 76.7% 有效 vs 单独 44.4%，Chazin 2021；AAC+NDBI 优于单用，Pope 2024）。
   - 支持家长上传自定义卡片（场景/实物/人物），自动并入 custom 词库。
3. **AI 绘本/阅读模块**：沿用原 PRD（6 页图文社交/情境故事、角色替换、朗读、收藏、家长审核）。

---

## 3. 前端需求（三端）

### 3.1 儿童端（Tablet 优先）
- 首页布局遵循截图与本 PRD。
- 🟢 Core Words 网格：运行态**位置锁定**；拖动排序**仅家长配置态可用**，保存后锁定。
- 句子条：一键朗读、删除最后一词。
- 🟢 顶部场景切换后：**仅** fringe 词库 + 场景背景更新、相关日程优先展示；**核心网格不变**。
- 任务提醒常驻右上角（可折叠）；Focus 模式保留 AAC 紧急按钮。

### 3.2 家长端（Mobile 优先）
> 沿用原 PRD：儿童档案、场景管理（VSD 热区）、日程管理、奖励设置、词库管理、生成绘本。
- 🔵 **补充**：词库管理需支持 **core（锁定布局）+ context + custom** 三类；**core 词位置编辑只在配置态生效**。
- 🟡 **补充**：选词向导默认 Universal Core 36，但引导家长按孩子**个体需求/偏好**增补 fringe。

### 3.3 治疗师端（Web）
> 沿用原 PRD：数据看板（AAC 使用频率、独立沟通率、任务完成率）、ABC 报表、词库/任务远程调整。
- 🟡 **补充**：看板指标应能支撑**个体化**词库调整决策（呼应 Carnett 2023 个体化建议）。

---

## 4. 后端需求

> 账户与权限、数据模型、服务模块、同步离线、安全合规整体沿用原 PRD 与 `CLAUDE.md` 架构（Neon Postgres、各域 service、ARASAAC 符号管线）。循证相关补充：

### 4.1 数据模型补充
- 🟢 `core_word_positions`（已存在）：核心词位置以**儿童为单位持久化且默认锁定**；需有 `locked` 状态字段，运行态读取不可写。
- 🟢 `vocabulary_items` 的 `core | context | custom` 分类保持；flashcard / AAC **引用同一 `vocabulary_items` 主键**，避免两套词。
- 🟢 `vsd_scenes` / `vsd_hotspots`：场景切换只影响 fringe 与背景，不触碰 core 布局。

### 4.2 服务模块补充
- 🟢 AAC 服务的"预测推荐"接口：**只返回高亮建议/推荐句，不返回新的位置坐标**（接口层面杜绝重排）。
- 🟢 学习内容服务：flashcard CRUD 与 vocabulary 打通；新增 custom 卡片自动写入 `vocabulary_items(type=custom)`。

### 4.3 离线与合规
> 沿用原 PRD：AAC 发声与日程提醒离线可用、本地缓存+增量同步、冲突以家长端为主；COPPA/GDPR、数据本地化、媒体加密。

---

## 5. 非功能性需求

> 沿用原 PRD：低刺激配色（鼠尾草绿/德国蓝）、柔和点击反馈、无过度动画、防误触、AAC 点击-发声 <200ms。

- ⚠️ **诚实标注**：以下为**合理工程默认值，非循证结论**，上线后应做可用性验证：
  - AAC 点击→发声延迟目标 `<200ms`。
  - 防误触"按住 0.5 秒触发"。
  - 本轮未找到这两项的循证量化阈值（列入开放问题）。

---

## 6. 版本规划（沿用原 PRD，循证优先级微调）

- **MVP（3 个月）**
  - 🟢 核心 AAC 网格（**固定位置** + Universal Core 36 全量可见）、句子条（逐词造句）、TTS。
  - 基础日程与任务提醒、代币奖励。
  - 🟢 Flashcards 基础版（**与 AAC 词库同源**）。
- **Phase 2（6 个月）**
  - 🟢 AI 高亮推荐（**不挪位**）。
  - 🟢 VSD 场景上传与热区（hybrid 显示）。
  - AI 绘本生成、Math 模块。
  - ⚠️ 预存快捷短语（高频社交/紧急少量）。
- **Phase 3（9-12 个月）**
  - 生物反馈情绪调节、治疗师端数据看板、场景感知切换（**仍只换 fringe，不动 core**）。

---

## 7. 循证约束清单（⛔ 不可妥协项）

开发与验收时逐条核对，违反即视为不符合循证设计：

1. ⛔🟢 核心词运行态**位置永不改变**（无自动重排、无 AI 换位、无运行态拖动）。
2. ⛔🟢 AI 只高亮/推荐句/朗读，**接口不返回新坐标**。
3. ⛔🟢 场景切换**只换 fringe + 背景**，core 网格不变。
4. ⛔🟢 flashcard 与 AAC **同一套 `vocabulary_items`**。
5. ⛔🔵 **不设能力门槛/先决技能**才给词汇；默认健壮词汇始终可见。
6. ⛔🔵 网格大小按"能看见+能点到"，**不按认知能力降级**。
7. ⛔ 逐词造句为主，预存短语只作补充。
8. ⛔ 凡产品/文档引用研究，按四档如实标注，不得把商业实践/共识夸大为强证据。

---

## 8. 证据出处索引

| 主题 | 出处 |
|------|------|
| AAC 不阻碍言语发展 | Millar/Light/Schlosser 2006 (JSLHR)；Schlosser & Wendt 2008 (AJSLP)；Yuan 2024 |
| 无年龄/无先决技能门槛 | ASHA 早期干预 Practice 页 |
| 固定位置利于运动规划 | Thistle 等 2018 (AJSLP)；LAMP（Center for AAC & Autism） |
| VSD 为 3-8 岁自闭症幼儿 EBP | Patenaude, McNaughton & Liang 2025 (JSET) |
| VSD 对幼儿初期优于网格 | Drager 等 2004 (JSLHR) |
| AAC+NDBI 更优 | Pope, Light & Laubscher 2024 (JADD) |
| 辅助输入/打包干预更有效 | O'Neill 等 2021 (AJSLP)；Chazin 等 2021 (AJSLP) |
| 核心词干预证据弱 + 个体化选词 | Carnett, Devine 等 2023 (Review J. Autism Dev. Disord.) |
| Universal Core 36 / 全量可见 | Project Core (UNC) |
| "80% 核心词" 原始出处 | Vanderheiden & Kelso 1987 |
| 网格大小不按认知降级 | AssistiveWare: Choosing a grid size |
| Fitzgerald 配色无疗效证据 | Communication Community |
| 预存句只作补充 | PrAACtical AAC；Trnka 等 2008（词预测） |

> 完整证据正文、可信度判定与可点击链接见 `docs/AAC_RESEARCH_EVIDENCE_2026-06.md`。
